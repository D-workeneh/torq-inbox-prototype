/** Layout offset for fixed overlays when island browser chrome is hidden. */
export const PHASE1_LAYOUT_TOP_OFFSET = 0;

/** Sidebar / content separator — kept for phase1 shell styling. */
export const PHASE1_CHROME_CONTENT_SEPARATOR = '#E1E3E1';
